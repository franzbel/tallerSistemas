<%= form_tag ({controller: "waiters", action: "create_order"}) do %>
    

    <% @comidas.each do |comida| %>
        <div id="checkbox">
          <div>
            <%= check_box_tag 'comida_ids[]', comida.id %>
          </div>
        </div>
       
        <div id="nombreComida">
          <div>
            <%= comida.name %>
          </div>
        </div>
       
        <div id="cantidades">
          <div>
          <%= number_field_tag 'cantidades[]', 0%>
          </div>
        </div>
        
        
        <div  id="cantidades">
          <div>
            <%= select_tag 'llevar[]', options_for_select([['si', 'si'], ['no', 'no']], 'no') %>            
          </div>
        </div>
        
        <div id="nombreComida">
          <div>
             Para llevar 
          </div>
        </div>
        
        <div id="observacion  ">
          <div>
            <%= text_area_tag 'observacion[]'%>            
          </div>
        </div>  
        
       <!-- <%= comida.availability %>-->
        <%= hidden_field_tag 'mesa_ids[]', @mesa.id %> 

      <br><br><br><br>    
    <% end %>
     
    <%= submit_tag 'Pedir comida'%>
    
<% end %>
  