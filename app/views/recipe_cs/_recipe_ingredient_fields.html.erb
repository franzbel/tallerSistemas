
<%= f.collection_select :ingredient_c_id, IngredientC.all, :id, :name, {prompt: "Agregar Ingrediente"}, {class: "form-control input-sm active"} %> 

              <%= f.link_to_remove "Eliminar Ingrediente", :class=> 'btn btn-default active' %>
           