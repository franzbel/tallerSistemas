
<%= nested_form_for @recipe_c do |f| %>
  <% if @recipe_c.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_c.errors.count, "error") %> prohibited this recipe_c from being saved:</h2>

      <ul>
      <% @recipe_c.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <h4><span class="label label-danger" <%= f.label :name, value='Nombre:'%></span></h4> 
    <%= f.text_field :name %>
  </div>

<h4><span class="label label-danger">Ingredientes: </span></h4>
 
<%= f.fields_for :recipe_ingredients %>
<br>
        <%= f.link_to_add "Agregar Ingrediente", :recipe_ingredients, :class=> 'btn btn-default active' %>|


<%= link_to 'Nuevo Ingrediente', new_ingredient_c_path(@recipe_c.id), :class=> 'btn btn-default ' %> 
  <div class="field">
     <h4><span class="label label-danger" <%= f.label :preparation, value='Preparacion:'%></span></h4>
    <%= f.text_area :preparation %>
  </div>

  <div class="actions">
    <%= f.submit "Aceptar"%>
  </div>
<% end %>




