<%= form_for(@table) do |f| %>
  <% if @table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% @table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status, 'Estado (*)' %><br>
    <%= f.select :status, ['libre', 'ocupada', 'limpieza', 'reservada']%>
  </div>
  <div class="actions">
    <%= f.submit "Crear Mesa", :class => "btn btn-success" %>
  </div>
<% end %>
